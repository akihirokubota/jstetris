name: test
description: aaa

inputs:
  service-names:
    default: "true"
    required: false
    description: aaa

runs:
  using: composite

  steps:
    - name: Login to Amazon ECR
      run: echo secret "${{secrets.TEST }}"



      
